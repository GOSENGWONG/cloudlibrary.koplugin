local _ = require("gettext")
return {
    name = "CloudLibrary",
    fullname = _("Cloud Library"),
    plugin_id = "cloud_library_plugin",
    description = _([[Sync books and metadata (annotations, reading progress, etc.) between devices.]]),
    version = "v1.4.3"
}
